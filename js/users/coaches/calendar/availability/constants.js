/* -- Class for the main block of the time filter. -- */
const timeFiltersClass = '.time-filters';

/* -- Class for inputs of time filters. -- */
const timeFiltersItem = '.time-filters-item';

/* -- Id button add additional time. -- */
const addAdditionalTimeId = '#add-additional-time';

/* -- String with the name of the input 'Type of time'. -- */
const typeOfTimeInput = 'input[name="type_of_time"]';

/* -- String with the name of the input 'All day'. -- */
const allDayInput = 'input[name="all_day"]';

/* -- String with the name of the input 'time_start_availability'. -- */
const startTimeFilterInput = 'input[name="time_start_availability"]';

/* -- String with the name of the input 'time_end_availability'. -- */
const endTimeFilterInput = 'input[name="time_end_availability"]';

/* -- String with the name of the class 'error-input'. -- */
const errorInputNameOfClass = 'error-input';

/* -- String with the name of the class 'input-disabled'. -- */
const disabledInputNameOfClass = 'input-disabled';

/* -- Id for block with selected groups. -- */
const selectedGroups = '#selected-groups';

/* -- Class for modal body availability. -- */
const setAvailabilityModalBody = '.set-availability-modal-body';

/* -- Confirm to continue editing. -- */
const overwriteChanges = 'Already have the selected changes, do you want to delete from and start editing?';

const closingWindowIfThereAreChanges = 'Are you sure you want to close the window? All changes will not be saved.';

/* -- String with the name of the class 'availability-input-time'. -- */
const availabilityInputTimeClass = '.availability-input-time';

/* -- String with the name of the class 'filters'. -- */
const filtersClass = '.filters';

/* -- String with the name of the id 'close-availability'. -- */
const closeAvailabilityId = '#close-availability';

/* -- Format for comparing dates in 12 hour format. -- */
const format12Hour = 'YYYY/MM/DD HH:mm A';

/* -- Format for dates. -- */
const formatDay = 'YYYY-MM-DD';

/* -- Tbody block in a table with calendar days. -- */
const tbodyCalendarClass = '.tbody-calendar';